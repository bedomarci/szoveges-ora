---
description: Run unit tests on the desktop (native) environment
---
1. Navigate to the Arduino test directory: `cd test/arduino`
// turbo
2. Run native tests: `pio test -e native`
> [!NOTE]
> This assumes you have a `native` environment configured in your `platformio.ini`.
