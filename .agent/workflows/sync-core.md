---
description: Synchronize core logic files to the Arduino library
---
1. Navigate to the Arduino test directory: `cd test/arduino`
// turbo
2. Run the sync script manually (optional, build handles it automatically): `python sync_core.py`
